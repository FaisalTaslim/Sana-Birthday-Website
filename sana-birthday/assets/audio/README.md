# Optional real music

Not required — the sound-toggle button uses a tiny procedural
tune generated in-browser by default. If you'd rather use a real
song, drop an mp3 here and see the "Using real music" section in
the top-level README.md for the two-line wiring change.
